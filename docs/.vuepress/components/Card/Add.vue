<template>
  <div>
    <p>
      To add a card, first make sure the page you're editing has a subfolder named 
      <code>Cards</code>. If not, right click on the current page in the content 
      directory, and select <code>New > Folder</code>. To add the new card, right 
      click on the <code>Card</code> folder, and select <code>New > OHA Card</code>.
    </p>
    <p>
      Now that your new card has been created, follow the steps in the 
      <a href="#card-fields">"Card Fields"</a> section below for more information 
      on configuring and editing the various card fields. Once you have filled out 
      each of the fields, go through the standard check-in process. Your new card 
      should now be visible on the site.
    </p>
  </div>
</template>
